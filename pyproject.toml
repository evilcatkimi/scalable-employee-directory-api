[project]
name = "hr-employee-search"
version = "1.0.0"
description = "HR Employee Search Microservice"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
